# Local Claude Code settings (machine-specific, not shared)
.claude/settings.local.json

# Secrets — never commit
.env
.env.*

# OS and editor cruft
.DS_Store
Thumbs.db
.vscode/

# Design source files — large Photoshop originals, not deployment assets.
# Exported web-ready PNG/JPG/WebP versions belong in brand_assets/ or
# assets/img/ instead; keep the .psd itself outside the repo.
*.psd
